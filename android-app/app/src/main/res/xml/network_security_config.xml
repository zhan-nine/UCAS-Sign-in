<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production: system CA only. iclass.ucas.edu.cn uses GlobalSign public CA. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Debug builds may add user CAs for local MITM debugging only. -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
